# Soukno Media Cache

Transient re-hosting for XHS video/image files, so Meta's Instagram Graph API
has a stable, non-expiring URL to fetch from during container creation
(XHS CDN links are signed and expire quickly, causing error 2207082).

Files here are overwritten/pruned periodically — do not rely on this repo
for permanent storage.
